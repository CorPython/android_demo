<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demostudy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SharedPreferences.Preferences"></activity>
        <activity android:name=".Fragment.ContainerActivity" />
        <activity android:name=".Handler.HandlerTestActivity" />
        <activity android:name=".RecycleView.LinearRecycleActivity" />
        <activity android:name=".RecycleView.RecycleViewActivity" />
        <activity android:name=".ListView.ListViewActivity" />
        <activity android:name=".ImageActivity" />
        <activity
            android:name=".Jump.B_Activity"
            android:label="B">
            <intent-filter>
                <action android:name="com.example.demostudy.Jump" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Jump.AActivity"
            android:label="A" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".buttonActivity" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>